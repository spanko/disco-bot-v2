name: discovery-chatbot-v2
metadata:
  template: disco-bot-v2
hooks:
  postprovision:
    shell: sh
    run: infra/scripts/post-provision.sh
services:
  api:
    project: .
    language: csharp
    host: containerapp
    docker:
      path: Dockerfile
